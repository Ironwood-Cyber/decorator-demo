{
  "type": "object",
  "properties": {
    "myArray": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string" },
          "name": {
            "type": "string",
            "minLength": 3,
            "description": "Please enter your name"
          },
          "age": { "type": "integer" },
          "birthday": { "type": "string", "format": "date" },
          "nationality": {
            "type": "string",
            "enum": ["DE", "IT", "JP", "US", "RU", "Other"]
          }
        },
        "required": ["id", "name"]
      }
    }
  }
}
